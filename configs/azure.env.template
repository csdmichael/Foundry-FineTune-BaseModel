# Azure Storage (Managed Identity — no connection string needed)
AZURE_STORAGE_ACCOUNT_URL=https://aistoragemyaacoub.blob.core.windows.net
AZURE_STORAGE_CONTAINER=kla-finetune

# Azure OpenAI / Foundry (Managed Identity — no API key needed)
AZURE_OPENAI_ENDPOINT=https://001-ai-poc.openai.azure.com/openai/v1
AZURE_OPENAI_API_VERSION=2024-10-21

# Latest fine-tunable chat base model for this POC
BASE_MODEL=gpt-4.1-mini

# Optional deployment for baseline evaluation (if different)
BASELINE_MODEL=gpt-4.1-mini
